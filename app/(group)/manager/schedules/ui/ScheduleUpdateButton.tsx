import React from "react";

import { TSchedule } from "@/types";

type Props = { record: TSchedule };

const ScheduleUpdateButton: React.FC<Props> = ({ record }) => {
	return <div>ScheduleUpdateButton</div>;
};

export default ScheduleUpdateButton;
